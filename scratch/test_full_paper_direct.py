import subprocess
import time

paper_text = """Random Features for Large-Scale Kernel Machines Ali Rahimi and Ben Recht Abstract To accelerate the training of kernel machines, we propose to map the input data to a randomized low-dimensional feature space and then apply existing fast linear methods. Our randomized features are designed so that the inner products of the transformed data are approximately equal to those in the feature space of a user specified shift-invariant kernel. We explore two sets of random features, provide convergence bounds on their ability to approximate various radial basis kernels, and show that in large-scale classification and regression tasks linear machine learning algorithms that use these features outperform state-of-the-art large-scale kernel machines. 1 Introduction Kernel machines such as the Support Vector Machine are attractive because they can approximate any function or decision boundary arbitrarily well with enough training data. Unfortunately, methods that operate on the kernel matrix (Gram matrix) of the data scale poorly with the size of the training dataset. For example, a dataset with half a million training examples might take days to train on modern workstations. On the other hand, specialized algorithms for linear Support Vector Machines and regularized regression run much more quickly when the dimensionality of the data is small because they operate on the covariance matrix rather than the kernel matrix of the training data [1, 2]. We propose a way to combine the advantages of the linear and nonlinear approaches. Inspired by randomized algorithms for approximating kernel matrices (e.g., [3, 4]), we efficiently convert the training and evaluation of any kernel machine into the corresponding operations of a linear machine by mapping data into a relatively low-dimensional randomized feature space. Our experiments show that random features combined with very simple linear learning techniques compete favorably with state-of-the-art kernel-based classification and regression algorithms. Random features significantly reduce the computation needed for training, and obtain similar or better testing error. The kernel trick is a simple way to generate features for algorithms that depend only on the inner product between pairs of input points. It relies on the observation that any positive definite function k(x, y) with x, y ∈ Rd defines an inner product and a lifting φ so that the inner product between lifted datapoints can be quickly computed as hφ(x), φ(y)i = k(x, y). The cost of this convenience is that algorithms access the data only through evaluations of k(x, y), or through the kernel matrix consisting of k applied to all pairs of datapoints. As a result, large training sets incur large computational and storage costs. Instead of relying on the implicit lifting provided by the kernel trick, we propose explicitly mapping the data to a low-dimensional Euclidean inner product space using a randomized feature map z : Rd → RD so that the inner product between a pair of transformed points approximates their kernel evaluation: k(x, y) = hφ(x), φ(y)i ≈ z(x) 0 z(y). (1) Unlike the kernel’s lifting φ, z is low-dimensional. Thus, we can simply transform the input with z, and then apply fast linear learning methods to approximate the answer of the corresponding nonlinear kernel machine. In what follows, we show how to construct feature spaces that uniformly approximate popular shift-invariant kernels k(x − y) to within  with only D = O(d−2 log 1  2 ) 1 dimensions, and empirically show that excellent regression and classification performance can be obtained for even smaller D. In addition to giving us access to extremely fast learning algorithms, these randomized feature maps also provide a way to quickly evaluate the machine. With the kernel trick, evaluating the machine at a test point x requires computing f(x) = PN i=1 cik(xi , x), which requires O(N d) operations to compute and requires retaining much of the dataset unless the machine is very sparse. This is often unacceptable for large datasets. On the other hand, after learning a hyperplane w, a linear machine can be evaluated by simply computing f(x) = w0z(x), which, with the randomized feature maps presented here, requires only O(D + d) operations and storage. We demonstrate two randomized feature maps for approximating shift invariant kernels. Our first randomized map, presented in Section 3, consists of sinusoids randomly drawn from the Fourier transform of the kernel function we seek to approximate. Because this map is smooth, it is wellsuited for interpolation tasks. Our second randomized map, presented in Section 4, partitions the input space using randomly shifted grids at randomly chosen resolutions. This mapping is not smooth, but leverages the proximity between input points, and is well-suited for approximating kernels that depend on the L1 distance between datapoints. Our experiments in Section 5 demonstrate that combining these randomized maps with simple linear learning algorithms competes favorably with state-of-the-art training algorithms in a variety of regression and classification scenarios. 2 Related Work The most popular methods for large-scale kernel machines are decomposition methods for solving Support Vector Machines (SVM). These methods iteratively update a subset of the kernel machine’s coefficients using coordinate ascent until KKT conditions are satisfied to within a tolerance [5, 6]. While such approaches are versatile workhorses, they do not always scale to datasets with more than hundreds of thousands of datapoints for non-linear problems. To extend learning with kernel machines to these scales, several approximation schemes have been proposed for speeding up operations involving the kernel matrix. The evaluation of the kernel function can be sped up using linear random projections [3]. Throwing away individual entries [3] or entire rows [4, 7, 8] of the kernel matrix lowers the storage and computational cost of operating on the kernel matrix. These approximations either preserve the separability of the data [4], or produce good low-rank or sparse approximations of the true kernel matrix [3, 7]. Fast multipole and multigrid methods have also been proposed for this purpose, but, while they appear to be effective on small and low-dimensional problems, to our knowledge, their effectiveness has not been demonstrated on large datasets. Further, the quality of the Hermite or Taylor approximation that these methods rely on degrades exponentially with the dimensionality of the dataset [9]. Fast nearest neighbor lookup with KD-Trees has been used to approximate multiplication with the kernel matrix, and in turn, a variety of other operations [10]. We compare our work to the Core Vector Machine (CVM), a state-of-the-art technique that takes an altogether different approach than those thus far discussed [12]. CVM transforms a classification problem into a support vector data-description problem, and solves this using a fast minimumenclosing ball algorithm that randomly samples the training data. Unlike previous work, instead of approximating the kernel matrix, our work approximates the kernel function directly. The feature map we present in Section 4 is reminiscent of KD-trees in that it partitions the input space using multi-resolution axis-aligned grids similar to those developed in [11] for embedding linear assignment problems. 3 Random Fourier Features Our first set of random features consists of random Fourier bases cos(ω 0x + b) where ω ∈ Rd and b ∈ R are random variables. These mappings project data points on a randomly chosen line, and then pass the resulting scalar through a sinusoidal function (see Figure 1 and Algorithm 1). Drawing the direction of these lines from an appropriate distribution guarantees that the product of two transformed points will approximate a desired shift-invariant kernel. 2 RD R2 ω x Kernel Name k(∆) p(ω) Gaussian e − k∆k 2 2 2 (2π) − D 2 e − kωk 2 2 2 Laplacian e −k∆k1 Q d 1 π(1+ω2 d ) Cauchy Q d 2 1+∆2 d e −k∆k1 Figure 1: Random Fourier Features. Each component of the feature map z(x) projects x onto a random direction ω drawn from the Fourier transform p(ω) of k(∆), and wraps this line onto the unit circle in R2 . After transforming two points x and y in this way, their inner product is an unbiased estimator of k(x, y). The mapping z(x) = cos(ω 0x + b) additionally rotates this circle by a random amount b and projects the points onto the interval [0, 1]. The table lists some popular shift-invariant kernels and their Fourier transforms. To deal with non-isotropic kernels, we can first whiten the data and apply one of these kernels The following classical theorem from harmonic analysis provides the key insight behind this transformation: Theorem 1 (Bochner [13]). A continuous kernel k(x, y) = k(x − y) on Rd is positive definite if and only if k(δ) is the Fourier transform of a non-negative measure. If a shift-invariant kernel k(δ) is properly scaled, Bochner’s theorem guarantees that its Fourier transform p(ω) is a proper probability distribution. Defining ζω(x) = e jω0x , we have k(x − y) = Z Rd p(ω)e jω0 (x−y) dω = Eω[ζω(x)ζω(y) ∗ ], (2) so ζω(x)ζω(y) ∗ is an unbiased estimate of k(x, y) when ω is drawn from p. Since both the probability distribution p(ω) and the kernel k(∆) are real, the integral (2) converges when the complex exponentials are replaced with cosines. Therefore, we may obtain a real-valued mapping that satisfies the condition E[zω(x)zω(y)] = k(x, y) by setting zω(x) = √ 2 cos(ω 0x+b), where ω is drawn from p(ω) and b is drawn uniformly from [0, 2π]. That zω(x)zω(y) has expected value k(x, y) is a consequence of the sum of angles formula. We can lower the variance of the estimate of the kernel by concatenating D randomly chosen zω into one D-dimensional vector z and normalizing each component by √ D. The inner product z(x) 0z(y) = 1 D PD j=1 zωj (x)zωj (y) is a sample average of zω and is therefore a lower variance approximation to the expectation (2). Since zω is bounded between + √ 2 and − √ 2 for a fixed pair of points x and y, Hoeffding’s inequality guarantees exponentially fast convergence in D between z(x) 0z(y) and k(x, y): Pr [|z(x) 0z(y) − k(x, y)| ≥ ] ≤ 2 exp(−D2/4). Building on this observation, a much stronger assertion can be proven for every pair of points in the input space simultaneously: Claim 1 (Uniform convergence of Fourier features). Let M be a compact subset of Rd with diameter diam(M). Then, for the mapping z defined in Algorithm 1, we have Pr  sup x,y∈M |z(x) 0 z(y) − k(y, x)| ≥   ≤ 2 8  σp diam(M)  2 exp  − D2 4(d + 2) , where σ 2 p ≡ Ep[ω 0ω] is the second moment of the Fourier transform of k. Further, supx,y∈M |z(x) 0z(y) − k(y, x)| ≤  with any constant probability when D = Ω  d  2 log σp diam(M)   . The proof of this assertion first guarantees that z(x) 0z(y) is close to k(x − y) for the centers of an -net over M × M. This result is then extended to the entire space using the fact that the feature map is smooth with high probability. See the Appendix for details. 3 By a standard Fourier identity, the scalar σ 2 p is equal to the trace of the Hessian of k at 0. It quantifies the curvature of the kernel at the origin. For the spherical Gaussian kernel, k(x, y) = exp −γkx − yk 2  , we have σ 2 p = 2dγ. Algorithm 1 Random Fourier Features. Require: A positive definite shift-invariant kernel k(x, y) = k(x − y). Ensure: A randomized feature map z(x) : Rd → RD so that z(x) 0z(y) ≈ k(x − y). Compute the Fourier transform p of the kernel k: p(ω) = 1 2π R e −jω0 δk(δ) d∆. Draw D iid samples ω1, · · · , ωD ∈ Rd from p and D iid samples b1, . . . , bD ∈ R from the uniform distribution on [0, 2π]. Let z(x) ≡ q 2 D [ cos(ω 0 1x+b1) ··· cos(ω 0 Dx+bD) ] 0 . 4 Random Binning Features Our second random map partitions the input space using randomly shifted grids at randomly chosen resolutions and assigns to an input point a binary bit string that corresponds to the bins in which it falls (see Figure 2 and Algorithm 2). Because it uses rectilinear grids, this mapping is well-suited for kernels that depend only on the L1 distance between pairs of points. The grids are constructed so that the probability that two points x and y are assigned to the same bin is proportional to k(x, y). The inner product between a pair of transformed points is proportional to the number of times the two points are binned together, and is therefore an unbiased estimate of k(x, y). 10000000 01000000 00100000 00010000 00001000 00000100 00000010 00000001 ≈ + + + · · · = k(xi , xj ) z1(xi) 0z1(xj ) z2(xi) 0z2(xj ) z3(xi) 0z3(xj ) z(xi) 0z(xj ) Figure 2: Random Binning Features. (left) The algorithm repeatedly partitions the input space using a randomly shifted grid at a randomly chosen resolution and assigns to each point x the bit string z(x) associated with the bin to which it is assigned. (right) The binary adjacency matrix that describes this partitioning has z(xi) 0 z(xj ) in its ijth entry and is an unbiased estimate of kernel matrix. We first describe a randomized mapping to approximate the “hat” kernel khat(x, y; δ) = max  0, 1 − |x−y| δ  on a segment of R, then show how to construct mappings for more general separable multi-dimensional kernels. Partition the real number line with a grid of pitch δ, and shift this grid randomly by an amount u drawn uniformly at random from [0, δ]. This grid partitions the real number line into intervals [u + nδ, u + (n + 1)δ] for all integers n. The probability that two points x and y fall in the same bin in this grid is max  0, 1 − |x−y| δ  [11]. In other words, if we number the bins in the grid so that a point x falls in bin xˆ = b x−u δ c and y falls in bin yˆ = b y−u δ c, then Pru[ˆx = ˆy|δ] = khat(x, y; δ). If we encode xˆ as a binary indicator vector z(x) over the bins, z(x) 0 z(y) = 1 if x and y fall in the same bin and zero otherwise, so Pru[z(x) 0 z(y) = 1|δ] = Eu[z(x) 0 z(y)|δ] = khat(x, y; δ). Therefore z is a random map for khat. Now consider shift-invariant kernels that can be written as convex combinations of hat kernels on a compact subset of R × R: k(x, y) = R ∞ 0 khat(x, y; δ)p(δ) dδ. If the pitch δ of the grid is sampled from p, z again gives a random map for k because Eδ,u[z(x) 0 z(y)] = Eδ [Eu[z(x) 0 z(y)|δ]] = Eδ[khat(x, y; δ)] = k(x, y). That is, if the pitch δ of the grid is sampled from p, and the shift u is drawn uniformly from [0, δ] the probability that x and y are binned together is k(x, y). Lemma 1 in the appendix shows that p can be easily recovered from k by setting p(δ) = δ ¨k(δ). For example, in the case of the Laplacian kernel, kLaplacian(x, y) = exp(−|x − y|), p(δ) is the Gamma distribution δ exp(−δ). For the Gaussian kernel, k(δ) is not convex, so ¨k is not everywhere positive and δ ¨k(δ) is not a probability distribution, so this procedure does not yield a random map for the Gaussian. 4 Random maps for separable multivariate shift-invariant kernels of the form k(x − y) = Qd m=1 km(|x m−y m|)(such as the multivariate Laplacian kernel) can be constructed in a similar way if each km can be written as a convex combination of hat kernels. We apply the above binning process over each dimension of Rd independently. The probability that x m and y m are binned together in dimension m is km(|x m − y m|). Since the binning process is independent across dimensions, the probability that x and y are binned together in every dimension is Qd m=1 km(|x m−y m|) = k(x−y). In this multivariate case, z(x) encodes the integer vector [ xˆ 1 ,··· ,xˆ d ] corresponding to each bin of the d-dimensional grid as a binary indicator vector. In practice, to prevent overflows when computing z(x) when d is large, our implementation eliminates unoccupied bins from the representation. Since there are never more bins than training points, this ensures no overflow is possible. We can again reduce the variance of the estimator z(x) 0 z(y) by concatenating P random binning functions z into an larger list of features z and scaling by p 1/P. The inner product z(x) 0z(y) = 1 P PP p=1 zp(x) 0 zp(y) is the average of P independent z(x) 0 z(y) and has therefore lower variance. Since z is binary, Hoeffding’s inequality guarantees that for a fixed pair of points x and y, z(x) 0z(y) converges exponentially quickly to k(x, y) as P increases. Again, a much stronger claim is that this convergence holds simultaneously for all points: Claim 2. Let M be a compact subset of Rd with diameter diam(M). Let α = E[1/δ] and let Lk denote the Lipschitz constant of k with respect to the L1 norm. With z as above, we have Pr  sup x,y∈M |z(x) 0 z(y) − k(x, y)| ≤   ≥ 1 − 36dP α diam(M) exp   −  P 2 8 + ln  Lk  d + 1   , The proof is analogous to that of Claim 1. Since the function z is piecewise constant over M, M × M can be broken up into a few small rectangular cells so that throughout each cell, k(x, y) does not change much and z(x) and z(y) do not change at all. With high probability, at the centers of these cells z(x) 0z(y) is close to k(x, y), which guarantees that k(x, y) and z(x) 0z(y) are close throughout M. (See Appendix). Note that α = R ∞ 0 1 δ p(δ) dδ = R ∞ 0 ¨k(δ) dδ is 1, and Lk = 1 for the Laplacian kernel. Algorithm 2 Random Binning Features. Require: A point x ∈ Rd . A kernel function k(x, y) = Qd m=1 km(|x m − y m|), so that pm(∆) ≡ ∆¨km(∆) is a probability distribution on ∆ ≥ 0. Ensure: A randomized feature map z(x) so that z(x) 0z(y) ≈ k(x − y). for p = 1 . . . P do Draw grid parameters δ, u ∈ Rd with the pitch δ m ∼ pm, and shift u m from the uniform distribution on [0, δm]. Let z return the coordinate of the bin containing x as a binary indicator vector zp(x) ≡ hash(d x 1−u 1 δ 1 e, · · · , d x d−u d δ d e). end for z(x) ≡ q 1 P [ z1(x)···zP (x) ] 0 . 5 Experiments In the first set of experiments, summarized in Table 1, we show that least squares regression on our random features is a fast way to approximate the training of supervised kernel machines. We restrict our attention to the CVM because it was shown in [12] to be both faster and more accurate than other known approaches, including, in most cases, random sampling of datapoints [4]. In this set of experiments, we trained regressors and classifiers by solving the least squares problem minw kZ 0w − yk 2 2 + λkwk 2 2 , where y denotes the vector of desired outputs and Z denotes the matrix of random features. To evaluate the resulting machine on a datapoint x, we can simply compute w0z(x). 5 Dataset Fourier+LS Binning+LS CVM Exact SVM CPU 3.6% 5.3% 5.5% 11% regression 20 secs 3 mins 51 secs 31 secs 6500 instances 21 dims D = 300 P = 350 ASVM Census 5% 7.5% 8.8% 9% regression 36 secs 19 mins 7.5 mins 13 mins 18,000 instances 119 dims D = 500 P = 30 SVMTorch Adult 14.9% 15.3% 14.8% 15.1% classification 9 secs 1.5 mins 73 mins 7 mins 32,000 instances 123 dims D = 500 P = 30 SVMlight Forest Cover 11.6% 2.2% 2.3% 2.2% classification 71 mins 25 mins 7.5 hrs 44 hrs 522,000 instances 54 dims D = 5000 P = 50 libSVM KDDCUP99 (see footnote) 7.3% 7.3% 6.2% (18%) 8.3% classification 1.5 min 35 mins 1.4 secs (20 secs) < 1 s 4,900,000 instances 127 dims D = 50 P = 10 SVM+sampling Table 1: Comparison of testing error and training time between ridge regression with random features, Core Vector Machine, and various state-of-the-art exact methods reported in the literature. For classification tasks, the percent of testing points incorrectly predicted is reported. For regression tasks, the RMS error normalized by the norm of the ground truth is reported. We base our comparison on the five standard large-scale datasets evaluated in [12], excluding the synthetic datasets. We replicated the results in the literature pertaining to the CVM, SVMlight, and libSVM using binaries provided by the respective authors.1 Despite the simplicity of our approach, least-squares with random features is faster than, and provides competitive accuracy with, alternative methods. It also produces very compact functions because only w and a set of O(D) random vectors or a hash-table of partitions need to be retained. Random Fourier features perform better on the tasks that largely rely on interpolation. On the other hand, random binning features perform better on memorization tasks (as measured by the number of support vectors needed by the SVM), because they explicitly preserve locality in the input space. This difference is most dramatic in the Forest dataset. Since the decision boundary for this problem is not smooth and requires tens of thousands of support vectors, the Fourier features perform quite poorly on this dataset. Figure 3(left) illustrates the benefit of training classifiers on larger datasets. When training data are easy to obtain, this provides an easy way to improve the accuracy of classifiers without additional modeling effort. Figure 3(middle) and (right) show that good performance can be obtained even from a modest number of features. 6 Conclusion We have presented randomized features whose inner products uniformly approximate many popular kernels, and demonstrated that these features are a powerful and economical tool for large-scale supervised learning. We showed empirically that providing these features as input to a standard linear learning algorithm produces results competitive with state-of-the-art kernel machines in accuracy, training time, and evaluation time. It is worth noting any mixture of these features (such as combining partitioning with Fourier features or sampling frequencies from mixture models) can be readily computed and applied to learning problems. An exciting direction for future research is to empirically and analytically evaluate random features on other learning tasks. While we have focused on regression and classification, our features can be applied to accelerate many other kernel methods, including semi-supervised and unsupervised learning tasks."""

# Let's ask the model to summarize the paper
prompt = paper_text + "\nBased on the text above, summarize the key contribution of the paper in one sentence:"

print(f"Generated prompt of length {len(prompt)} characters (~{len(prompt)//4} tokens)")

start = time.time()
res = subprocess.run(
    ["./diffkv_native/build/diffkv_native", "diffkv_native/qwen2.5-1.5b-instruct-q8_0.gguf", prompt],
    capture_output=True,
    text=True
)
dur = time.time() - start

print(f"\nFinished in {dur:.2f} seconds.")
print("=== STDOUT ===")
print(res.stdout)
print("=== STDERR ===")
stderr_lines = res.stderr.splitlines()
for line in stderr_lines[-30:]:
    print(line)
