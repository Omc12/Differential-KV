# ── Core ─────────────────────────────────────────────────────────────────────
torch>=2.1.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0
tqdm>=4.65.0
tabulate>=0.9.0
scikit-learn>=1.3.0
fastapi
uvicorn
pydantic
sse-starlette
psutil
pyyaml
httpx>=0.27.0
# CUDA attention interception (runtime/dkv_attention.py) monkeypatches the
# 4.x Qwen2Attention.forward signature. transformers 4.48+/5.x reorder the args
# (position_embeddings positional; use_cache/position_ids removed) → SILENT
# GARBAGE output. Keep transformers on 4.x until the patch is ported to 5.x.
# (MLX/Mac path only uses transformers for the tokenizer and is unaffected.)
transformers>=4.43,<4.48
accelerate>=0.29.0

# ── CUDA only (Linux / Windows / cloud) ──────────────────────────────────────
# triton>=2.1.0          # GPU Triton kernels — install manually on CUDA machines
# torchao>=0.3.0         # INT8/INT4 weight quantization — CUDA only

# ── Apple Silicon (macOS) — optional performance extras ──────────────────────
# mlx>=0.16.0            # Apple MLX — faster SVD on M-series chips
#                          Install with: pip install mlx
# torchao>=0.3.0         # torchao INT8 on MPS (experimental, PyTorch >= 2.3)
#                          Install with: pip install torchao
