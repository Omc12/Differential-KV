\section{Implementation Notes}
\label{sec:impl}

\dkv{}'s active runtime is $\approx6{,}000$ lines of Python over MLX~\cite{mlx2023}, evaluated on
Qwen2.5-1.5B~\cite{qwen25}. A few implementation choices are load-bearing and worth recording,
because they are where correctness or performance was actually won or lost.

\begin{sloppypar}
\paragraph{Attention by monkey-patch.} Rather than fork \texttt{mlx\_lm}, the runtime
replaces \path{qwen2.Attention.__call__} at load time with the patched forward of
\S\ref{sec:overview}, and hands each layer a reference to the shared block manager.
This keeps the runtime a thin, model-agnostic overlay on an unmodified upstream model.

\paragraph{Randomized SVD in NumPy.} Compression runs the randomized truncated SVD
in fp32 in NumPy on the CPU, not in MLX on the GPU. On unified memory this is nearly
free to marshal (no host--device copy), and it sidesteps GPU-only limitations in the
current MLX linear-algebra path (e.g.\ \texttt{qr}); a $255\times512$ block factorizes
in $\approx2$\,ms. The seed is fixed so that compression---and therefore decode output
and needle recovery---is deterministic run to run; an unseeded range finder made recall
vary between identical runs.
\end{sloppypar}

\paragraph{Decode precision.} The decode score/merge arithmetic uses fp16 products with fp32
log-sum-exp accumulation. A well-intentioned change that recast the merge operands to fp32 for
``overflow safety'' was measured to shift the combine by roughly fp16-epsilon and \emph{regress}
long-context ($16$k/$32$k) needle recall into a repetition loop, because at those lengths the
retrieval margin sits on a knife edge. The fp32-log-sum-exp / fp16-combine split is the validated
configuration and is enforced by a parity oracle in the test suite.

\paragraph{Router/attention decoupling.} The number of residuals the router \emph{scores}
(Eq.~\eqref{eq:router}) is a separate knob from the number a block \emph{stores}. The router cost
is $O(\text{nb}\cdot R_{\text{route}}\cdot\dhead)$ per token and dominates long-context decode, so
$R_{\text{route}}$ defaults to $\min(64, \maxres{})$ rather than the full storage budget; raising
\maxres{} for prose fidelity therefore does not automatically inflate router cost.

\paragraph{Layer-adaptive rank.} By default, the SVD rank assigned to each layer varies with
depth: early layers ($\leq 25\%$) use $0.75r$, middle layers ($25$--$75\%$) use $1.5r$, and late
layers ($\geq 75\%$) use $0.5r$, where $r{=}32$ is the base rank. The pool is sized at the
maximum rank $1.5r{=}48$; layers use only what they need. Attention quality is more
rank-sensitive in middle layers (higher information density), while early and late layers tolerate
more aggressive compression. The base rank $r{=}32$ is the value cited throughout.

\paragraph{Multi-signal residual selection.} Residual tokens are selected by a boosted-priority
ranking rather than plain reconstruction error. Three IDF-weighted boost signals are active by
default: \emph{owner-capture} (entity names accompanying high-error values), \emph{edge-capture}
(relational connectives whose low-rank reconstruction collides with a neighbour's key, detected
by a cosine-collision test), and a \emph{coverage bonus} (uniform spread across block positions
so a single high-error segment cannot monopolise all slots). All three are individually
controllable via environment flags; together they preserve the entity-name and relational
structure that the error signal alone cannot reliably surface.

\paragraph{Configuration.} Behavior is controlled by environment flags with safe defaults, so new
paths ship disabled until validated. The defaults used throughout this report are: compressed
sparse decode on; fused decode buffer on (\texttt{DKV\_DECODE\_FUSED}); block-sparse prefill
on; base rank $\rank{=}32$ with layer-adaptive variation; $\maxres{=}128$; top-\topk{} $=16$;
residual-key router; SVD seed $1234$. The optimized dense baseline is \texttt{mlx\_lm} with a full
KV cache (chunked prefill, prefill-to-decode allocator release) on the same int4 weights; the
standard PyTorch baseline is \texttt{AutoModelForCausalLM} on the PyTorch MPS backend with an
uncompressed full KV cache and none of those memory-management paths.

\paragraph{Gated subsystems.} A factual-store / semantic-retrieval module, a lego streaming
prefill (memory-bounded prefill that drops raw KV after block compression), a speculative-decode
draft path, and a query-aware instruction-pinning mechanism (\texttt{DKV\_INSTR\_PIN}) exist in the
codebase but are disabled by default (they are no-ops in the configuration measured here) and are
not part of the evaluated system. Query-aware instruction pinning identifies high-IDF query tokens
pre-prefill to statically pin answer-candidate blocks as always-attended attention sinks during
block-sparse prefill, avoiding question-block pruning when instructions appear mid-document; we
implement this as an architectural extension for future long-context instruction-following ablations.
We mention these subsystems only to be exact about what is and is not exercised.


\paragraph{Runtime optimizations inspired by kTransformers.}
\label{par:ktransformers}
The active runtime incorporates four throughput and memory optimizations whose design is informed
by the kTransformers inference framework~\cite{yang2025ktransformers}, adapted to \dkv{}'s
compressed-block layout.

\emph{(i) Tiered CPU--GPU KV offloading.}
kTransformers demonstrates that heterogeneous CPU--GPU memory tiers can sustain long-context
inference by keeping hot layers in GPU memory while cold weights reside in CPU
RAM~\cite{yang2025ktransformers}. We apply the analogous idea at the \emph{block} level: each
pool slot carries a heat score $h \leftarrow 0.7\,h\,\gamma + 0.3\,\rho$ (decay $\gamma{=}0.95$,
routing signal $\rho{\in}[0,1]$) updated at every decode step.
When pool occupancy exceeds a threshold (default $80\%$), the coldest slots are evicted
to pinned CPU RAM and their GPU memory is reclaimed. Because \dkv{}'s SRL chunk-graph router
selects the same block neighborhood across consecutive decode steps, the same $\topk$ blocks
remain hot and almost never enter the cold tier during steady-state generation; offloading
triggers only for blocks that have not been routed for many steps, i.e.\ genuinely irrelevant
history. Re-routing to a previously cold block requires a host-to-device (H2D) transfer; for a
default block (${U}$: $63{\times}32$ int8; ${V_K,V_V}$: $2{\times}32{\times}\Hkv{\times}\dhead$
fp16; anchors: fp16) the total is ${\approx}137$\,KB, costing ${\approx}15\,\mu$s on PCIe~4.0.
At the decode cadence of $20$--$80$\,ms per token this is below $1\%$ overhead even for a cold
hit, and Feature (ii) below hides it entirely.

\emph{(ii) Step-ahead asynchronous block prefetch.}
After the router delivers the top-$\topk$ slot indices for step $t$, a background daemon thread
submits non-blocking H2D restores for any cold slots in that set, so they are warm before step
$t{+}1$ begins. The submit path is a lock-free \path{queue.put_nowait} call
(${\approx}50\,\text{ns}$) and does not touch the critical path.
On \cuda{}, the restore issues on a dedicated \texttt{stream\_h2d} stream; after issuing the
\path{cudaMemcpyAsync} the dispatcher records a \path{cudaEvent} on \texttt{stream\_h2d} and
calls \path{cudaStreamWaitEvent} on the main compute stream, so the kernel launch on step $t{+}1$
automatically stalls only as long as the H2D transfer is outstanding---no global device
synchronization. On \mlxe{}/MPS the transfer is synchronous but still below the decode step
budget. In practice, because routing is stable across steps, the prefetch almost always completes
before it is needed, making the effective H2D overhead zero.

\emph{(iii) Kernel-level tiling for the expansion step.}
On CUDA, the main fused decode kernel (\S\ref{sec:decode}) is wrapped with
\path{@triton.autotune}~\cite{tillet2019triton}, which benchmarks a grid of
(\path{S_MAX}, \path{BLOCKS_PER_CHUNK}, \path{num_warps}) configurations on the
first five calls and thereafter uses the fastest for each $(\rank, \dhead)$ shape. This gives
full inner-loop unrolling and register reuse matched to the specific rank and GPU. On Apple
Silicon the analogous acceleration uses \path{mx.fast.metal_kernel} to tile the $U V$
expansion across Metal threadgroups, matching the throughput profile of a hand-written
tiled matmul; it falls back to standard \path{mx.matmul} if the MLX version does not
expose the custom-kernel API.

\emph{(iv) MLA-style latent projection (experimental, disabled by default).}
DeepSeek-V2~\cite{deepseekv2} introduces Multi-head Latent Attention (MLA), which projects
keys and values into a shared low-rank latent space before storage, reducing KV cache footprint
without architecture changes~\cite{deepseekv2}. We incorporate an analogous pre-SVD projection:
a learned matrix $W\in\R^{d_{\mathrm{feat}}\times d_L}$ (with $d_L{\ll}d_{\mathrm{feat}}$,
default $d_L{=}d_{\mathrm{feat}}/4$) projects the per-block delta matrix to a latent space
before the SVD, exposing a more concentrated singular spectrum and reducing the effective rank
needed to reach the energy threshold. $W$ is initialised from an online PCA over the first
$n_{\mathrm{calib}}$ blocks (default 16), after which it is frozen. The right singular vectors
are unprojected back to full $d_{\mathrm{feat}}$ before pool storage so the attention kernel
interface is unchanged. Before $W$ converges the projection is the identity, preserving
correctness during warm-up. Because the additional reconstruction error (${\approx}0.1$--$0.5\%$)
compounds with the SVD approximation, this feature is disabled by default
(\path{DKV_MLA_LATENT=0}) and requires NIAH recall validation before production use.

\paragraph{Testing.} A kernel-parity oracle checks the fused decode kernel against a reference
implementation on seeded sessions; the needle and relational-binding harnesses guard recall. The
measurement scripts, prompts, and the exact commands are in the reproducibility appendix\ifext{}
(Appendix~\ref{app:repro})\fi.

\subsection{\cuda{}-specific implementation notes}
\label{sec:cuda-impl}

% Figure f8 (native CUDA call chain) planned; to be added when diagram is finalised.
\IfFileExists{figures/f8_native_cuda.pdf}{%
\begin{\figenv}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/f8_native_cuda.pdf}
  \caption[Native C++/GGML decode path: prefetch stream, fused kernel, and error
  checking.]{Native C++/GGML decode path. \path{execute_cuda_attention()} waits on the
  event recorded by the background \path{stream_h2d} prefetch, so the H2D block restore
  (\S\ref{sec:background-cuda}, Figure~\ref{fig:memtopology}) is already in flight a step
  ahead. The kernel body performs the same anchor score, delta projection and separate
  K/V in-place residual scatter as the Triton path (Figure~\ref{fig:triton}) and returns
  through the same interface---the two are one algorithm in two languages, differing only
  in the split-K scratch allocation and the \path{DKV_CUDA_CHECK} wrapper. \pendingGPU{}}
  \label{fig:nativecuda}
\end{\figenv}
}{}


The kTransformers-informed optimizations above (\S\ref{par:ktransformers}) span both backends.
This subsection records three \cuda{}-only design decisions that arise from the discrete-GPU
memory model and have no \mlxe{} equivalent.

\paragraph{Triton compilation pipeline and strict-mode telemetry.}
\label{par:triton-telemetry}
At import time, \path{triton_fused_decode.py} checks \path{HAS_TRITON}; if true, the kernel
is compiled with \path{@triton.jit} on the first call. A one-time log line---\textbf{``Triton
path ACTIVE''}---confirms the live kernel is executing rather than the PyTorch fallback.
This distinction is critical for GPU regression testing: a numerically wrong kernel that does
not throw an exception is otherwise invisible, and a performance measurement taken on the
PyTorch path would silently undercount \cuda{} throughput. \path{DKV_TRITON_STRICT=1}
converts the \path{except Exception} fallback into a hard re-raise, making such failures
explicit. The default (\path{STRICT=0}) preserves safe degradation in production environments
where Triton is absent.

\paragraph{GPU-native SVD via the Gram-Eigh reformulation.}
\label{par:gram-eigh}
As described in \S\ref{sec:background-cuda} and \S\ref{sec:compression}, \cuda{} compression
can run the same CPU-offloaded NumPy SVD as \mlxe{}, or a GPU-native path activated by
\path{DKV_COMPRESS_GRAM_SVD=1}. The reformulation computes the $r{\times}r$ Gram matrix
$B B^\top$ and calls \texttt{cuSOLVER} \texttt{syevjBatched}. Two additional flags tune the
trade-off: \path{DKV_RSVD_MAX_RPROJ=32} caps the Gram-matrix dimension at $32$ for
\texttt{syevjBatched} compatibility (larger ranks fall back to CPU), and
\path{DKV_CONTIGUOUS_PREFILL=1} / \path{DKV_CONTIG_UNROTATE=1} activate the $1{\times}$
prefill variant described in \S\ref{sec:runtime} that eliminates the 2$\times$ HBM spike.
GPU recall validation of the Gram-Eigh path is pending \pendingGPU{}.

\paragraph{\cuda{} Graphs: roadmap and current blockers.}
\label{par:cuda-graphs}
\cuda{} Graphs would allow the per-token decode sequence to be captured as a single replayed
graph, eliminating per-kernel launch overhead. The current implementation cannot use them
because host Python synchronization points---\path{tensor.item()} and \path{routing.tolist()}
calls in the routing hot path---occur mid-graph and require host--device round trips that
prevent graph capture. The planned migration is a three-phase sequence: (1)~replace all
\path{.item()} / \path{.tolist()} calls in the critical decode path with async-safe GPU
predicates so the routing decision stays on device; (2)~isolate the graph boundary to the
pure-kernel sequence (query projection, Triton fused kernel, inter-chunk reduction kernel);
(3)~validate graph replay bit-exactness against eager mode on seeded sessions before enabling
for production. No step in this roadmap requires changes to the block representation or
compression algorithm.
