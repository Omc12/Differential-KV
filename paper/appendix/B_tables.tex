\section{Full Measured Results}
\label{app:tables}

Table~\ref{tab:detail} lists every cell of the fresh primary sweep
(\texttt{paper/scripts/measure\_sweep.sh} and \texttt{measure\_tail.sh}) for both engines, exactly
as recorded by the isolated workers. \emph{MLX peak} is the allocator peak
(\texttt{get\_peak\_memory}) inside the worker process; \emph{Prompt tok} is the tokenizer count
for the built NIAH prompt at that target. Empty cells are measurements that do not apply; failed
cells would be stated as failures in the text, never given an invented value.

\begin{table}[h]
\centering
\caption{Per-run detail for the primary sweep: \diffkv{} (active) and the dense baseline on
identical int4 weights. All values measured.}
\label{tab:detail}
\small
\input{tables/t5_detail}
\end{table}

\noindent The configuration table (Table~\ref{tab:config}) records the model and runtime
parameters as read from the code, and Table~\ref{tab:budget} in \S\ref{sec:compression} derives the
per-block byte budget from those same dimensions.

\begin{table}[h]
\centering
\caption{Model and \diffkv{} configuration, as measured/loaded by the runtime.}
\label{tab:config}
\small
\input{tables/t1_config}
\end{table}
